# this is note; properly known as a "comment"
x <- 2 + 2
y <- x * 2

# Descriptive statistics --------------------------------------------------

# because you can use them when learning R
# to make notes to yourself about what everything
# does ....

z <- x + y

# Visualization stuff -----------------------------------------------------

x <- rnorm(100)

x <- rnorm(
  100
  )
